1. (D) Q1 **Agent architecture**
   According to the definition used in this course, which of the following
   components may be part of an AI agent architecture?
   - [ ] Guardrails
   - [?] System prompt
   - [?] Model
   - [ ] Runtime
   - [ ] Context window
   - [?] Tools
   - [ ] Planning skills
   - [ ] Memory
2. (E) Q2 **Strengths and weaknesses of LLMs**
   You are developing a system based on an LLM without access to external tools or memory.
   Which of the following tasks will the model most likely perform reliably?
   - [+] Paraphrase a text while preserving its meaning
   - [-] Count 'r' in "strawberry"
   - [+] Generate a short summary of a long article
   - [+] Explain the difference between two programming concepts
3. (F) Q3 **Special tokens and tool calling**
   A developer accidentally removes the special tokens that mark the start and
   end of tool calls from the tool-description prompt section, but keeps
   the tool description and the tool result text in the prompt.
   What is the most likely consequence?
   - [+] The LLM may start treating tool outputs as normal conversation text
     and become less reliable at deciding when to call tools
4. (G) Q4 **Tool Calling**
    In an agent system, an LLM has access to a tool get_weather(city).
    A user asks: “What is the weather in Berlin right now?”
   - [+] The LLM outputs a structured tool call; the agent executes the tool
      and sends the result back to the model
5. (H) Q5 **Best Practices for Tools**
   An agent uses the following tool: book_flight(user_id, flight_id)
   Internally, the tool performs several actions:
   - charges the user’s payment method
   - reserves a seat on the flight
   - creates a booking record
   During execution, the system successfully charges the payment,
   but fails before creating the booking record.
   The agent then retries the tool call. How should the tool be designed to
   prevent an inconsistent system state (for example, charging the user twice)?
   - [+] The tool should check whether the booking or payment already exists
     before executing again
6. (I) Q6 **MCP Architecture**
   Which of the following statements correctly describe the roles in the MCP architecture?
   Please select all that apply
   [+] A host creates MCP clients to connect to MCP servers
   [+] MCP servers execute tools and return structured results
   [+] A single host can connect to multiple MCP servers using clients
   [ ] A single client can connect to multiple MCP servers
23. (Z) Q23 What should you do (about latency)? 
    Your agent solves tasks with 90% success rate, but average latency is 25 seconds.
    Users abandon after ~8 seconds. What should you do?
    - [ ] Add caching for repeated queries
    - [+] Break tasks into multi-step pipeline with fast routing + slow reasoning only when needed
    - [ ] Add loading indicator so users wait longer
24. AA  **Refund**
    Your agent uses an LLM to decide whether to refund a customer.
    It sometimes approves clearly invalid refunds. What is the best fix?
    - [-] Add more examples to the prompt
    - [+] Move refund decision logic to deterministic riules and use LLM only for interpretation 
